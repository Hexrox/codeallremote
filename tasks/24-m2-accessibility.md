# M2 tasks — accessibility and test matrix

## M2-20 — Android accessibility acceptance

Run the accessibility checklist on Home, Session, Approval, Changes and Settings. Record device/API level, font scale, TalkBack result and remediation.

## M2-21 — Cross-layer test matrix

Implement or document the cases in `docs/59-test-matrix.md`; map every failure case to a deterministic fixture, integration test or signed manual runbook result.

