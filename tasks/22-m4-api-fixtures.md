# M4 tasks — API fixtures

## M4-08 — Publish endpoint fixtures

Create request/response/error fixtures for every M1/M2 endpoint and validate them against the OpenAPI/JSON Schema contract.

## M4-09 — Add protocol compatibility tests

Verify additive fields, unknown event handling, unsupported protocol rejection and cursor-expired resync using the documented fixture set.

