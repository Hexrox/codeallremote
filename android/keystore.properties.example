# Copy to android/keystore.properties (gitignored) to sign release builds.
# When absent, CI and dev builds still produce debug + unsigned release.
storeFile=/absolute/path/to/car-release.keystore
storePassword=CHANGE_ME
keyAlias=car
keyPassword=CHANGE_ME
